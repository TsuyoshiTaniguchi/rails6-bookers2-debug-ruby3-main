$("#book_<%= @book.id %>").find(".favorite-btn").replaceWith("<%= j(render 'favorites/favorite-btn', book: @book) %>");
$("#book_<%= @book.id %>").find(".favorites-count").html("<%= @book.favorites.count %>");
